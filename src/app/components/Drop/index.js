// import Upload from '../../components/UploadImages/Upload';


// handleUploaded = () => {
  
// }


// export default Drop = () => {
//   <Upload />
// }




// const uploadFiles =  setFiles([...files]);
      // // uploadFiles.forEach(processUpload);




      // function updateFile(id, data) {
      //   setFiles({ uploadFiles: setFiles.uploadFiles.map(uploadedFile => {
      //     return id === uploadedFile.id 
      //     ? { ...uploadedFile, ...data } 
      //     : uploadedFile;
      //   }) })
      // }


      // function processUpload(uploadFile) {
      //   const data = new FormData();

      //   data.append('images_post', uploadFile.file, uploadFile.name);

      //   api.post('post', data, {
      //     onUploadProgress: e => {
      //       const progress = parseInt(Math.round((e.loaded * 100) / e.total)); 

      //       updateFile(uploadFile.id, {
      //         progress,
      //       });
      //     }
      //   })
      //   .then((res) => {
      //     console.log(`A imagem ${uploadFiles.name} foi enviada com sucesso`);
      //   })
      //   .catch((err) => {
      //     console.log(`Houve um problema ao realizar o upload da imagem ${uploadFiles.name} no servidor`);
      //     console.log(err);
      //   })
      // } 

